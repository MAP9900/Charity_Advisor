[phases.setup]
aptPkgs = ['git-lfs']

[phases.install]
cmds = [
  'git -C .. lfs install',
  'git -C .. lfs fetch',
  'git -C .. lfs checkout',
  'pip install -r requirements.txt'
]

[start]
cmd = 'uvicorn app:app --host 0.0.0.0 --port $PORT'
